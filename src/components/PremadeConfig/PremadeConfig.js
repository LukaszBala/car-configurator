import React from 'react';
import PropTypes from 'prop-types';
import './PremadeConfig.scss';

const PremadeConfig = () => (
  <div className="PremadeConfig">
    PremadeConfig Component
  </div>
);

PremadeConfig.propTypes = {};

PremadeConfig.defaultProps = {};

export default PremadeConfig;
